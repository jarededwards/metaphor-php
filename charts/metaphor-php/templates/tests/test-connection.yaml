apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metaphor-php-php-php-php.fullname" . }}-test-connection"
  labels:
{{ include "metaphor-php.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "metaphor-php.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
